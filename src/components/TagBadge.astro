---
interface Props {
  tag: string;
}
const { tag } = Astro.props;
---

<a href={`/tag/${tag}`} class="inline-block rounded-full px-2 py-0.5 text-xs font-medium bg-zinc-100 dark:bg-zinc-800 text-zinc-700 dark:text-zinc-300 hover:text-blue-600 dark:hover:text-blue-400">
  {tag}
</a>
